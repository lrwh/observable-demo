server:
  port: 8080
api:
  url: http://localhost:${server.port}
spring:
  application:
    name: 付款单
  mvc:
    view:
      prefix: /
      suffix: .html
extra:
  host: localhost
trace:
  exporter:
    host: localhost
    port: 14250
    uiPort: 16686
client: false